#ifndef SECONDPRACTICAL_QLEARNING_H
#define SECONDPRACTICAL_QLEARNING_H


#include "State.h"

class Qlearning {
private:
    int epochs;
public:
    Qlearning(int epochs) : epochs(epochs) {
        // initialize Q(s,a) arbitrarily
    }
    void run(State s) {
        // Initialize s

        // Repeat for each step of this episode
    }
};


#endif //SECONDPRACTICAL_QLEARNING_H
